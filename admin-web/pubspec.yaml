name: sistema_vistorias_admin
description: Painel Administrativo do Sistema de Vistorias

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter

  # State Management
  get: ^4.6.5
  
  # UI Components
  flutter_admin_scaffold: ^1.2.0
  data_table_2: ^2.5.8
  fl_chart: ^0.64.0
  
  # Firebase
  firebase_core: ^2.24.0
  firebase_auth: ^4.15.0
  cloud_firestore: ^4.13.3
  
  # Utils
  intl: ^0.18.1
  url_launcher: ^6.2.1
  file_picker: ^6.1.1
  universal_html: ^2.2.4
  
  # HTTP
  http: ^1.1.0
  dio: ^5.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
    - assets/icons/
